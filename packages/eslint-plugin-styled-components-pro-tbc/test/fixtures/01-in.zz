import baz from './bar';

const foo = styled(Table)`
height: 100%;
> tbody > tr {
  width: 100%;
  border-bottom: 1px solid black;
  &:last-child {
    border-bottom: none;
  }
  `;

const bar = "bar";
