test(`01`, () => {
  mixer().forEach((opt, n) => {
    equal(a(), "b", `01.01.${pad(n)}`);
  });
  mixer().forEach((opt, n) => {
    equal(c(), "d", `01.02.${pad(n)}`);
  });
  mixer().forEach((opt, n) => {
    equal(e(), "f", `01.03.${pad(n)}`);
  });
});
