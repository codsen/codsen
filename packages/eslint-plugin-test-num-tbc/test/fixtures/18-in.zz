test(`09 - with entities`, () => {
  mixer({
    convertDashes: true,
    convertEntities: true,
    removeWidows: false,
  }).forEach((opt, n) => {
    equal(convertOne(``, opt), [[4, 5, "&rsquo;"]], "9");
    equal(convertOne(``, opt), [[4, 5, "&rsquo;"]], '9');
    equal(convertOne(``, opt), [[4, 5, "&rsquo;"]], `9`);
  });
});

test(`09 - with entities`, () => {
  mixer({
    convertDashes: true,
    convertEntities: true,
    removeWidows: false,
  }).forEach((opt, n) => {
    equal(convertOne(``, opt), [[4, 5, "&rsquo;"]], "9");
    equal(convertOne(``, opt), [[4, 5, "&rsquo;"]], '9');
    equal(convertOne(``, opt), [[4, 5, "&rsquo;"]], `9`);
  });
});

test(`09 - with entities`, () => {
  mixer({
    convertDashes: true,
    convertEntities: true,
    removeWidows: false,
  }).forEach((opt, n) => {
    equal(convertOne(``, opt), [[4, 5, "&rsquo;"]], "9");
    equal(convertOne(``, opt), [[4, 5, "&rsquo;"]], '9');
    equal(convertOne(``, opt), [[4, 5, "&rsquo;"]], `9`);
  });
});
