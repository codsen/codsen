test(`01`, () => {
  mixer({}).forEach((opt, n) => {
    equal(
      a(),
      `b`,
      "01.01"
    );
  });
  mixer({}).forEach((opt, n) => {
    equal(
      a(),
      `b`,
      "01.02"
    );
  });
});
