test.skip(`01 - ${`\u001b[${36}m${`traverse`}\u001b[${39}m`} - traversal continues after the hole`, () => {
  equal(
    extract({}),
    {},
    "01.01"
  );
});

test.only(`02 - b`, () => {
  equal(
    extract({}),
    {},
    `02.01`
  );
  equal(
    extract({}),
    {},
    "02.02"
  );
});

test.run();