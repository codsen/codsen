{
  "private": true,
  "name": "@codsen/design-tokens",
  "version": "0.2.0",
  "description": "Codsen design tokens",
  "author": "Roy Revelt <roy@codsen.com>",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "directories": {
    "src": "src"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "rimraf ./dist && node ./scripts/build",
    "lint": "eslint . --fix",
    "postinstall": "yarn run build",
    "prepare": "echo 'ready'",
    "test": "yarn run build",
    "unit": "yarn run build"
  },
  "devDependencies": {
    "change-case": "^4.1.2",
    "fs-extra": "^10.0.0",
    "rimraf": "^3.0.2",
    "style-dictionary": "^3.1.1"
  }
}
