{
  "name": "@codsen/data",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./index.tsx",
  "scripts": {
    "build": "rimraf ./dist && tsc index.ts --module commonjs --outDir dist --declaration",
    "postinstall": "yarn run build"
  },
  "devDependencies": {
    "@types/node": "^17.0.23",
    "prettier": "^2.6.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.4"
  }
}
