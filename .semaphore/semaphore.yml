version: v1.0
name: JavaScript example
agent:
  machine:
    type: e1-standard-4
    os_image: ubuntu1804
blocks:
  - name: Permissions
    prologue:
      commands:
        - checkout
        - node --version
        - npm --version
    task:
      jobs:
        - name: Bootstrap everything
          commands:
            - pwd
            - ls
