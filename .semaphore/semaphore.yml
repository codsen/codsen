version: v1.0
name: JavaScript example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Permissions
    task:
      prologue:
        commands:
          - checkout
          - node --version
          - npm --version
      jobs:
        - name: Bootstrap everything
          commands:
            - pwd
            - ls
        - name: Part 2
          commands:
            - pwd
            - ls
