{
  "name": "codsen",
  "private": true,
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "fresh": "rm -rf node_modules && lerna clean -y && npm run bootstrap",
    "build": "lerna run test --bail --concurrency 2",
    "cm": "git-cz",
    "pub": "lerna publish",
    "license": "make clone-license"
  },
  "devDependencies": {
    "commitizen": "^3.0.5",
    "cz-conventional-changelog": "^2.1.0",
    "lerna": "^3.6.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
